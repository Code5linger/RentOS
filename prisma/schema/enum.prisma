enum Role {
    OWNER
    TENANT
    ADMIN
}

enum LeaseStatus {
    ACTIVE
    ENDED
}

enum InvoiceStatus {
    PENDING
    PARTIAL
    PAID
    LATE
}

enum PaymentStatus {
    INITIATED
    SUCCESS
    FAILED
}

enum PaymentMethod {
    CASH
    BANK_TRANSFER
    MOBILE_BANKING // bKash, Nagad, Rocket
    CHEQUE
}
